<div class="container">
  <h1>My Tasks</h1>
  <br>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task.id) %></li>
    <% end %>
  </ul>
  <br>
    <button>
      <%=link_to "add a task", new_task_path %>
      </button>
  </div>
