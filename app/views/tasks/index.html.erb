<div class="container mt-2">
  <h1>My Tasks</h1>
  <br>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task.id) %></li>
    <% end %>
  </ul>
  <br>
      <span class="add-task">
      <%=link_to "+ Add a task", new_task_path %>
      </span>
  </div>
